# Append to /opt/wacrm/deploy/Caddyfile then reload Caddy.
# Domain: lakoku.biz.id (+ app.) → reverse_proxy lakoku-web:5200 on wacrm_edge

lakoku.biz.id, app.lakoku.biz.id {
	encode gzip
	reverse_proxy lakoku-web:5200
}
