# Task 2 — Modular Characterization Detectors (`lib/narrative-qa/`)

## Context
M10-A Story Bible dataflow audit (branch `audit/m10-a-story-bible-dataflow`, base `b7961311cf70b91cb7245149e400075c4e454d74`). Commit `9e1f804` already ships the audit contract types in `lib/narrative-qa/story-bible-audit-contract.ts` — read it FIRST; every detector's finding must conform to `StoryBibleAuditFinding` and `StructuredEvidence`/`EvidenceClass` there.

## Constraints (binding)
- PURE modules only. Never import server-only modules (anything touching Supabase/admin DB client, e.g. `lib/supabase/*`, `lib/narrative/server.ts`, `lib/narrative/loader.ts`, `lib/runtime/personalized-generation.ts`). Source-level evidence = STRING references (file path + symbol + observation), never live imports.
- No hardcoded findings. Findings are EMITTED by deterministic rules from input data (e.g. `if (latestExpected > latestVisible) emit('CHOICE_HISTORY_RECENT_LOSS')`). Do NOT pre-push findings because the plan suspects them.
- No production code edits. Only create files under `lib/narrative-qa/`.
- No new dependencies. TypeScript strict mode, no `as any`, no `@ts-ignore`.

## Deliverables
Create these files (split logic, do not force one monolith):
1. `lib/narrative-qa/choice-history-audit.ts` — analyzers for `CHOICE_HISTORY_RECENT_LOSS`, `CHOICE_HISTORY_DUPLICATE_PREVIOUS`, `CHOICE_HISTORY_BUDGET_PRESSURE` over a list of choice-history items (each: `chapterNumber`, `label`, `consequence`, `effectSummary`, `flags`). Budget model: token estimate = chars/4; detector fires BUDGET_PRESSURE when estimated tokens exceed declared budget (input param, default e.g. 2500).
2. `lib/narrative-qa/context-pressure-audit.ts` — analyzers over a synthetic growing canon (facts with `isLoadBearing`, threads, timeline events, act rollups): `CONTEXT_DECLARED_BUDGET_OVERSHOOT` (actual used > declared), `LOAD_BEARING_PRESSURE` (load-bearing cost high vs budget), `RELEVANT_FACT_EVICTION` (facts excluded while budget tight), `ROLLUP_EVICTION_PRESSURE` (rollups excluded). Input: a `CanonContextSample` interface you define (chapter, declaredBudget, facts[], threads[], timeline[], actRollups[], choiceHistory[]).
3. `lib/narrative-qa/blueprint-audit.ts` — `BLUEPRINT_VERSION_RESOLUTION_DIVERGENCE`: compare versions resolved by runtime vs compiler vs brief from a list of blueprint versions (interface: `{chapterNumber, version, source: 'runtime'|'compiler'|'brief', beats[]}`); emit divergence when resolved versions differ.
4. `lib/narrative-qa/thread-audit.ts` — `THREAD_ADVANCEMENT_SIGNAL_DISCONNECTED` (draft `advancedThreadIds` empty while threads expected to advance, or actual draft signals never reach validator — represent as input flag), `THREAD_OPEN_SIGNAL_DISCONNECTED` (`opensNewThread` false while new thread exists), `THREAD_STALENESS_NOT_LOAD_BEARING` (thread `stale` flag exists but context selection ignores it). Trace REAL production path as evidence strings: `lib/runtime/personalized-generation.ts` builds `threadContext` with `advancedThreadIds: []` / `opensNewThread: false`; validator lives in `lib/narrative/continuity-checks.ts` / `lib/narrative/threads.ts` — read these files (they are pure-ish; import allowed ONLY if no server deps) and cite exact symbols.
5. `lib/narrative-qa/plot-debt-audit.ts` — `PLOT_DEBT_PROGRESS_NOT_PERSISTED`, `PLOT_DEBT_CLOSE_NOT_PERSISTED`, `PLOT_DEBT_NEXT_CHAPTER_STATE_STALE`, `PLOT_DEBT_MILESTONE_MEMORY_GAP`. Trace real production path: `lib/story-engine/plot-debt.ts` (pure), `lib/story-engine/plot-debt-closure.ts`, StoryContractSchema `plotDebts` (openedChapter, milestones[{chapter,target}], closedChapter, status). Read these files for evidence.
6. `lib/narrative-qa/ending-audit.ts` — `ENDING_LOCK_NOT_DURABLE`, `ENDING_LOCK_RETRY_DIVERGENCE` (retry of chapter 45 resolves different ending), `ENDING_LOCK_POST45_SWITCH` (ending changes after lock chapter 45), `ENDING_LOCK_WORKER_LEGACY_PARITY_RISK`. Read `lib/story-engine/ending-resolver.ts` for evidence (pure module). Ending fixture interface: `{chapterNumber, resolvedEndingId|null, lockedEndingId|null}`.
7. `lib/narrative-qa/propagation-audit.ts` — trace StoryContract fields (`corePromise`, `mainConflict`, `finalQuestion`, `chapterTargets[n]`, `emotionalTurn`, `expectedThreadMovement`, `plotDebts`, `endingCandidates`, `closureRunway`) through ChapterBrief → PreProseBrief → writer prompt. Evidence strings from real files: `lib/story-engine/chapter-brief.ts`, `lib/story-engine/pre-prose-brief.ts`, `lib/prose/prompt-engine/build-writer-prompt.ts`, `lib/ai-gateway/gateway.ts`. Emit findings when a field is persisted but never prompt-visible. Detectors: `DEPENDENCY_DECLARED_BUT_UNUSED`, `RETRIEVAL_LOG_WRITE_PATH_UNPROVEN`, `CONTEXT_PACKET_CONSUMER_UNPROVEN` (hypothesis checks, INFO severity unless evidence says otherwise).
8. `lib/narrative-qa/act-rollup-audit.ts` — act rollup lifecycle: who creates (evidence: `supabase/migrations/20260707000000_core_runtime_baseline.sql :: act_rollups`), when updated, who reads (`lib/narrative/loader.ts :: loadCanonSnapshot` source-string evidence), whether summary reaches writer after compile (`lib/narrative/compiler.ts :: compileContext`). Statuses: `DEAD_PATH_CANDIDATE` / `CONSUMER_UNPROVEN` based on evidence found.
9. `lib/narrative-qa/chapter50-audit.ts` — finalization: `FINAL_STATE_RECONCILIATION_GAP`, `FINAL_CHAPTER_DUPLICATE_STATE_RISK`, `FINAL_READER_STATE_STALE`. Input: sequence of publish attempts `{attempt, success}`; detector characterizes deterministic vs best-effort reconciliation.
10. `lib/narrative-qa/story-bible-audit.ts` — public entrypoint: `runStoryBibleAudit(inputs)` aggregating all modules into `AuditReportArtifact` (executionStatus SUCCESS/ERROR, auditVerdict PASS/HOLD — HOLD iff any BLOCKER or HIGH finding, summary counts, matrix rows, findings). Also export `buildSourceOfTruthMatrix()` returning `DomainSourceMatrixRow[]` for all 17 domains (Character, Voice, Facts, Knowledge, Secret, Timeline, Thread, Act Rollup, Blueprint, Story Contract, Reader Route, Choice History, Ending, Plot Debt, Chapter, Checkpoint, Retrieval) — statuses assigned from actual evidence gathered while reading the code, with `evidence: StructuredEvidence[]`.

## Report
Write report to `.superpowers/sdd/task-2-report.md` with: files created, each detector + when it fires, real production symbols cited (file :: symbol), statuses assigned in matrix, typecheck result (`pnpm typecheck`), lint result for new files (`pnpm lint`), any concerns.

## Return contract (final message)
status: DONE | DONE_WITH_CONCERNS | BLOCKED | NEEDS_CONTEXT — plus one-line test summary and concerns, if any.
