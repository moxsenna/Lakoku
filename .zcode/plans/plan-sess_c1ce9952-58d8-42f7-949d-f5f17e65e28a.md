## Phase 4: Personalisasi Opsi Quick Onboarding

### Koreksi vs Plan Sebelumnya

| # | Koreksi |
|---|---|
| 1 | Jangan ubah/tambah `defaultAnswer` — pakai `promoteOptions()` reorder saja |
| 2 | Freeze questions dengan `activeQuestions` state saat quick flow dimulai |
| 3 | Jangan truncate default options — jika >=6 hanya reorder, jika <6 inject max 6 |
| 4 | Profile kosong/skipped tidak mempersonalisasi — `hasUsableTasteProfile()` guard |

### File DIUBAH (2 file, 0 baru)

**1. `lib/onboarding/question-presets.ts`**

Implementasi `getStorySetupQuestions(profile)`:
- Helper: `hasUsableTasteProfile(profile)` — cek `completedAt`, `preferredGenres`, `likedTropes` non-empty, skip kalau `skippedAt` without `completedAt`
- Helper: `cloneQuestions()` — deep copy options array
- Helper: `promoteOptions(options, preferred)` — sort: yang match naik
- Helper: `injectOptions(options, injected, max=6)` — tambah opsi baru jika slot tersedia, jangan duplikat
- Helper: `demoteAvoidedOptions(options, avoided)` — sort: yang match avoided turun
- Helper: `normalizeToken(token)` — lowercased trimmed untuk fuzzy matching

Mapping personalisasi:

| Field profile | Efek | Syarat |
|---|---|---|
| `preferredGenres` | Inject/promote opsi konflik di question `trope` | Selalu |
| `likedTropes` | Promote opsi yang mirip | Selalu |
| `avoidedTropes` | Demote opsi mirip (jangan hapus) | Selalu |
| `dramaIntensity` | Promote opsi `sikap` | Hanya jika `completedAt` |
| `romanceLevel` | Promote opsi `hubungan` | Hanya jika `completedAt` |
| `endingBias` | Promote opsi `akhir` | Hanya jika `completedAt` |

Aturan capacity:
- Default options >= 6 → hanya reorder, jangan inject
- Default options < 6 → inject maksimal sampai total 6
- Tidak pernah menghapus opsi default

**2. `components/mulai/onboarding-flow.tsx`**

Tambah state:
```ts
const [activeQuestions, setActiveQuestions] = useState<SetupQuestion[]>([])
```

Handler `Mulai cepat`:
```ts
function startQuickFlow() {
  setEntryMode('quick')
  setActiveQuestions(getStorySetupQuestions(tasteProfile))
  setStep(0)
  setPhase('quiz')
}
```

Semua referensi `questions` di quiz pakai `activeQuestions` (bukan `defaultStorySetupQuestions`).

Import: ganti `defaultStorySetupQuestions` menjadi `getStorySetupQuestions`.

### Smoke test (tambah ke file existing)

**`scripts/taste-profile-smoke.ts`** — tambah 8 check:
- `getStorySetupQuestions(null)` = same as default
- Skipped profile tidak mempersonalisasi
- Profile kosong tidak mempersonalisasi
- `preferredGenres` inject opsi jika slot tersedia
- Base options tidak terhapus semua
- `avoidedTropes` demote ke bawah
- `romanceLevel: 'utama'` promote opsi romance
- `endingBias: 'keadilan'` promote opsi keadilan

### Yang TIDAK disentuh
- Engine, schema, actions, draft, /brainstorm

### Urutan Eksekusi
1. Implement `getStorySetupQuestions()` di `question-presets.ts`
2. Update `onboarding-flow.tsx`: `activeQuestions` state + `startQuickFlow()`
3. Tambah 8 smoke check ke `taste-profile-smoke.ts`
4. Verify: typecheck + smoke + unit test