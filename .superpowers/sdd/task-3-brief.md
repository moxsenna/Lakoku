# Task 3 — Narrative QA Unit Test Suite (`tests/narrative-qa/`)

## Context
M10-A Story Bible dataflow audit (branch `audit/m10-a-story-bible-dataflow`, base `b7961311cf70b91cb7245149e400075c4e454d74`). Detectors already exist in `lib/narrative-qa/` (10 modules: choice-history-audit, context-pressure-audit, blueprint-audit, thread-audit, plot-debt-audit, ending-audit, propagation-audit, act-rollup-audit, chapter50-audit, story-bible-audit with runStoryBibleAudit + buildSourceOfTruthMatrix). Read the modules BEFORE writing tests — your tests exercise their REAL exported input interfaces.

## Constraints (binding)
- Tests only under `tests/narrative-qa/`. NO production edits, NO server-only imports, NO new deps, no `as any`/`@ts-ignore`.
- Audit tests verify DETECTOR CORRECTNESS, not Lakoku health. A detector finding (e.g. CHOICE_HISTORY_RECENT_LOSS) is the EXPECTED OUTPUT of a correctly-working detector — tests must PASS when the detector fires correctly. Tests go red only when detector logic misbehaves (wrong code, wrong severity, non-determinism, crash).
- Pure fixtures from `fixtures/long-horizon/story-bible-pressure.ts` may be used where shapes line up; otherwise construct minimal inline fixtures matching each module's input interface. (Note: fixture `ChoiceHistoryItemFixture.consequence` is string but detector `ChoiceHistoryItem.consequence` is string[] — adapt when using.)

## Deliverables (10 test files)
1. `tests/narrative-qa/story-bible-audit.test.ts` — runStoryBibleAudit: executionStatus SUCCESS with valid inputs; auditVerdict HOLD when a BLOCKER/HIGH finding present, PASS when none; ERROR status when an input group throws (if module supports that); matrix has exactly 17 rows, all statuses from allowed AuditStatus set; retrieval logging hypothesis check surfaced (RETRIEVAL_LOG_WRITE_PATH_UNPROVEN emitted when retrievalLogInvoked=false, and NOT emitted when true).
2. `tests/narrative-qa/context-pressure.test.ts` — growing facts/load-bearing/rollups across milestones (use chapter milestones 1/10/20/30/35/40/45/48/49/50 — may sample a subset if full set is heavy, but at least 10, 30, 45, 50); detectors CONTEXT_DECLARED_BUDGET_OVERSHOOT (actual > declared), LOAD_BEARING_PRESSURE (load-bearing cost >= 25% budget), RELEVANT_FACT_EVICTION + ROLLUP_EVICTION_PRESSURE fire on the stress cases totalBudget=4000 with loadBearingCost 900/1500/3000/4500 (vary sample to trigger); overshoot does NOT auto-fail — it is characterization; detectorsTriggered on milestones non-empty when detectors fire (regression for fixed buildContextPressureMilestone).
4. `tests/narrative-qa/choice-history-pressure.test.ts` — 49 realistic choices (generateSyntheticChoices(49)); pressure at 10/20/30/40/50; detect recent-choice truncation (CHOICE_HISTORY_RECENT_LOSS fires when newest visible chapter < expected latest); duplicate detection (feed consecutive duplicate label+consequence → CHOICE_HISTORY_DUPLICATE_PREVIOUS); budget pressure at high chapter counts (CHOICE_HISTORY_BUDGET_PRESSURE fires when chars/4 estimate > declaredBudget); full 49-choice set does not lose the newest choice at 4096-char summary slice (characterize).
5. `tests/narrative-qa/blueprint-version-audit.test.ts` — multi-version resolution: same chapter with v1/v2 where runtime resolves v2 (version desc) but brief resolves v1 (find() first) → BLUEPRINT_VERSION_RESOLUTION_DIVERGENCE; when all resolve same version → no finding.
6. `tests/narrative-qa/plot-debt-lifecycle-audit.test.ts` — per plan §10: Bab 5 debt OPEN → Bab 10 progressed (per-milestone progress present) → Bab 20 second milestone → Bab 35 close (ledger closed) → Bab 36 reload contract (status stale) → PLOT_DEBT_NEXT_CHAPTER_STATE_STALE; closure not persisted → PLOT_DEBT_CLOSE_NOT_PERSISTED; milestone memory gap fires only when SOME milestones progressed; mustProgressBy due without progress → PLOT_DEBT_PROGRESS_NOT_PERSISTED.
7. `tests/narrative-qa/ending-lock-parity-audit.test.ts` — per plan §12: Bab 44 lockedEndingId null → Bab 45 resolve A + lock → Bab 46+ resolves stay A (no ENDING_LOCK_POST45_SWITCH) → retry Bab 45 with different candidate → ENDING_LOCK_RETRY_DIVERGENCE (BLOCKER); chapter >= 45 resolved but not locked → ENDING_LOCK_NOT_DURABLE; v2 publishPath at 45 → ENDING_LOCK_WORKER_LEGACY_PARITY_RISK.
8. `tests/narrative-qa/thread-signal-audit.test.ts` — validatorReceivesDraftSignals=false with expected advances → THREAD_ADVANCEMENT_SIGNAL_DISCONNECTED; opensNewThread=false while new thread present → THREAD_OPEN_SIGNAL_DISCONNECTED; stale threads ignored by selection → THREAD_STALENESS_NOT_LOAD_BEARING; all signals correctly wired → no findings.
9. `tests/narrative-qa/act-rollup-lifecycle-audit.test.ts` — rollups seeded at act boundary, never updated → DEAD_PATH_CANDIDATE; rollups in compiled packet but writer prompt lacks rollup section → CONSUMER_UNPROVEN; rollups reaching prompt → no findings.
10. `tests/narrative-qa/writer-propagation-audit.test.ts` — StoryContract fields corePromise/mainConflict/finalQuestion persisted but never prompt-visible → DEPENDENCY_DECLARED_BUT_UNUSED (HIGH); field propagated contract→brief→prompt → no finding; chapterTargets[n]/emotionalTurn/expectedThreadMovement propagation trace; CONTEXT_PACKET_CONSUMER_UNPROVEN emitted when packet consumer unproven and absent when proven.
11. `tests/narrative-qa/chapter50-finalization-audit.test.ts` — ch50 published + reader state SELESAI → no findings; publish ok but reader state not SELESAI → FINAL_READER_STATE_STALE; publish fails once then recovers → characterized (no BLOCKER if deterministic recovery); duplicate success attempts → FINAL_CHAPTER_DUPLICATE_STATE_RISK.

## Test conventions
Match existing repo test style: Vitest, `describe`/`it`, Indonesian+English comments ok. Look at an existing test file (e.g. tests/ dir) for imports/format. Deterministic only — no timers, no random, no network, no env access.

## Report
Write report to `.superpowers/sdd/task-3-report.md`: files created, test counts, which plan §18 acceptance rows covered, `pnpm test:unit -- tests/narrative-qa` result (or `pnpm exec vitest run tests/narrative-qa`), `pnpm typecheck`, `pnpm exec eslint tests/narrative-qa lib/narrative-qa` results, concerns. Do NOT commit.

## Return contract (final message)
status: DONE | DONE_WITH_CONCERNS | BLOCKED | NEEDS_CONTEXT — plus one-line test summary and concerns.